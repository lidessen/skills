---
"agent-worker": minor
---

Rewrite daemon as multi-agent HTTP service, replace Unix socket IPC with HTTP server, add RESTful routes and WebSocket streaming, rename CLI commands, improve stability and reliability
