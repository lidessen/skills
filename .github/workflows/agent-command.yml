name: Agent Command

on:
  issue_comment:
    types: [created]

jobs:
  dispatch:
    # Only run on PR comments from repo owner that start with /agent-
    if: |
      github.event.issue.pull_request &&
      github.event.comment.user.id == github.event.repository.owner.id &&
      startsWith(github.event.comment.body, '/agent-')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
    steps:
      # Use env var to avoid script injection from comment body
      - name: Parse command
        id: parse
        env:
          COMMENT_BODY: ${{ github.event.comment.body }}
        run: |
          COMMAND=$(echo "$COMMENT_BODY" | head -1 | grep -oP '^/agent-[a-z0-9-]+' | sed 's|^/||')
          if [ -z "$COMMAND" ]; then
            echo "No valid command found"
            exit 0
          fi
          echo "command=$COMMAND" >> "$GITHUB_OUTPUT"
          echo "Parsed command: $COMMAND"

      - name: Get PR head SHA
        if: steps.parse.outputs.command != ''
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          SHA=$(gh pr view ${{ github.event.issue.number }} \
            --repo ${{ github.repository }} \
            --json headRefOid --jq '.headRefOid')
          echo "head_sha=$SHA" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        if: steps.parse.outputs.command != ''
        with:
          ref: ${{ steps.pr.outputs.head_sha }}

      # Resolve workflow file: .ts takes priority over .yaml
      - name: Resolve workflow file
        if: steps.parse.outputs.command != ''
        id: resolve
        env:
          COMMAND: ${{ steps.parse.outputs.command }}
        run: |
          if [ -f ".workflows/${COMMAND}.ts" ]; then
            echo "file=.workflows/${COMMAND}.ts" >> "$GITHUB_OUTPUT"
            echo "type=ts" >> "$GITHUB_OUTPUT"
          elif [ -f ".workflows/${COMMAND}.yaml" ]; then
            echo "file=.workflows/${COMMAND}.yaml" >> "$GITHUB_OUTPUT"
            echo "type=yaml" >> "$GITHUB_OUTPUT"
          else
            echo "file=" >> "$GITHUB_OUTPUT"
            echo "type=none" >> "$GITHUB_OUTPUT"
          fi

      - name: React to comment
        if: steps.resolve.outputs.file != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api repos/${{ github.repository }}/issues/comments/${{ github.event.comment.id }}/reactions \
            -f content='eyes'

      - uses: oven-sh/setup-bun@v2
        if: steps.resolve.outputs.file != ''
        with:
          bun-version: latest

      - name: Install dependencies
        if: steps.resolve.outputs.file != ''
        run: bun install

      - name: Build agent-worker
        if: steps.resolve.outputs.file != ''
        working-directory: packages/agent-worker
        run: bun run build

      # .ts workflows: run directly with bun
      - name: Run workflow (ts)
        if: steps.resolve.outputs.type == 'ts'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.issue.number }}
        run: |
          bun ${{ steps.resolve.outputs.file }}

      # .yaml workflows: run via agent-worker CLI
      - name: Run workflow (yaml)
        if: steps.resolve.outputs.type == 'yaml'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.issue.number }}
        run: |
          bun packages/agent-worker/src/cli/index.ts run ${{ steps.resolve.outputs.file }}

      - name: Report unknown command
        if: steps.parse.outputs.command != '' && steps.resolve.outputs.type == 'none'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMMAND: ${{ steps.parse.outputs.command }}
        run: |
          gh pr comment ${{ github.event.issue.number }} \
            --repo ${{ github.repository }} \
            --body "Unknown command: \`/$COMMAND\`. No workflow found at \`.workflows/${COMMAND}.ts\` or \`.workflows/${COMMAND}.yaml\`."
