interface:
  display_name: "Context Engineering"
  short_description: "Deliver the right project context at the right moment"
  default_prompt: "Use $context-engineering to audit how this project context reaches the agent and affects its decision."
