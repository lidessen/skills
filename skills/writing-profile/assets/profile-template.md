# 写作画像 · {{user_name_or_id}}

> 由 skill 内部的 finalize 阶段生成（所有单元完成后由 dispatch 自动触发），依据 `~/.claude/writing-profile/units/` 下的测试结果综合计算。
> 这是当前写作**偏好**的总结，不是身份标签——随着你写作的演化可以 `/writing-profile W<N>` 更新单个维度然后重新 finalize。

Generated: {{timestamp}}
Based on: {{N}}/7 completed dimensions
Last updated: {{timestamp}}

---

## 你是：**{{TYPE_LABEL}}** · **{{中文类别}}**

**{{作家谱系}}** — 最接近的两位作家是 **{{作家 A}}** × **{{作家 B}}**。

{{一句话描述 — 基于 archetype，不要 boilerplate}}

> {{一段 50-100 字描述，描述此 type 在写作中表现出什么习惯和倾向}}

---

## 坐标 (consumer contract — 下游 skill 读此)

```yaml
# Writer-BTI 7 维向量，-10 到 +10。按三层构念分组。

# L1 认知启动 (cognitive operation — 最稳定)
S_N: {{signed_int}}        # W1: positive = N (intuition / 抽象), negative = S (sensing / 具象)
T_F: {{signed_int}}        # W2: positive = T (thinking / 逻辑), negative = F (feeling / 感受)

# L2 文本姿态 (textual stance — 中等稳定)
# 注意：字母借用 MBTI 但构念已重定义为写作专属，不等同 MBTI 人格结果
E_I: {{signed_int}}        # W3: positive = E (发言朝外), negative = I (发言朝内)
J_P: {{signed_int}}        # W4: positive = J (文本收束), negative = P (文本发散)

# L3 审美偏好 (aesthetic preference — 需要暴露前提)
D_L: {{signed_int}}        # W5: positive = Dense (致密), negative = Light (疏朗)
G_H: {{signed_int}}        # W6: positive = Grave (严肃), negative = Humor (反讽)
C_V: {{signed_int | null}} # W7: positive = Classical (文言), negative = Vernacular (白话)
                           # 条件维度；null = 古典暴露度不足，副字默认 V 侧

# 古典暴露度（不进入类型，仅供下游 skill 参考）
classical_exposure: {{high | medium | low}}

# 类型标签
type: {{5-letter code, e.g., "ENTP-R" or "ENTP-R*" if C_V null}}
subtype_cn: {{中文类别, e.g., "杂文家"}}
archetype: {{full archetype name with writer reference}}

confidence:
  S_N: {{high | medium | low}}
  T_F: {{high | medium | low}}
  E_I: {{high | medium | low}}
  J_P: {{high | medium | low}}
  D_L: {{high | medium | low}}
  G_H: {{high | medium | low}}
  C_V: {{high | medium | low | na}}  # na when not measured
```

---

## 各维度详解（人类可读）

### W1 感官-直觉 (S/N) → {{score}} {{letter}}

{{一句描述，例如："你明显偏直觉 (N+7)：比起具体场景，你更被抽象模式和概念吸引。"}}

{{如果置信度低：⚠️ 这一维度置信度 {{confidence}}，建议 `/writing-profile W1` 重测以得到更稳定结果。}}

### W2 理性-感性 (T/F) → {{score}} {{letter}}
...

### W3 外倾-内倾 (E/I) → {{score}} {{letter}}
...

### W4 收束-发散 (J/P) → {{score}} {{letter}}
...

### W5 致密-疏朗 (D/L) → {{score}}
...

### W6 严肃-反讽 (G/H) → {{score}}
...

### W7 文言-白话 (C/V) → {{score | "未测" (degraded/skipped)}}
...

---

## 机械反 AI 清单

下游写作 skill 在修改扫描阶段机械应用这些规则。这些是根据你的坐标自动生成的——不是通用建议。

### 必删短语/结构

{{based on vector, list specific phrases to remove, e.g.:}}
- 删除: "综上所述"、"值得注意的是"、"总的来说"、"不难看出"、"可以看到"（所有用户都删）
- 删除: "在某种程度上"、"可以说"、"也许可以认为"（因为你的 T 强，避免过度 hedge）
- 避免: "进行 + 动词"结构（"进行分析"、"进行探讨"等；因为你的 V 强）
- 避免: "对……进行……"句式（西化中文标志；你偏 V）
- 压缩: 任何超过 25 字的单句，拆成两句（因为你的 L 强）

### 必审查模式

- 三段式排比：如非语义必需，改写成更自然的表达（你偏 {{?}}）
- 四字词堆砌：每段只保留 1 个自然嵌入的四字词（你的 C_V 偏 V）
- 段落均匀：若连续 5 段字数差都小于 10%，刻意加入一两段短段打破节奏

### 保留 / 鼓励

{{based on strong-side signals:}}
- 保留反讽/黑色幽默段落，哪怕看起来偏激——这是你的声音（H 信号强）
- 鼓励第一人称，不要改为"有人/我们/读者" 等替代称呼（E/I 看偏好）
- 允许跨领域比喻（N 信号强）

---

## 作家锚点座标图

**距你最近（类似你的写作）**：
1. {{作家 A}} — 距离 {{d}} — 在 {{主要接近维度}} 上高度一致
2. {{作家 B}} — 距离 {{d}}
3. {{作家 C}} — 距离 {{d}}

**和你最远（你大概不会这么写）**：
1. {{作家 X}} — 距离 {{d}}
2. {{作家 Y}} — 距离 {{d}}

---

## 注意

这份档案是**当前偏好**，不是身份标签。写作偏好会随时间缓慢变化，尤其是：
- 你刚开始写新体裁时
- 你刚经历强烈阅读体验（一本让你惊艳的书）后
- 你有意训练新风格（例如想写更克制/更发散的东西）时

建议：每年重跑 1-2 个你觉得最可能变化的维度，用 `/writing-profile W<N>` 单独更新。

---

Generated by `writing-profile` skill. Source data: `~/.claude/writing-profile/units/*.md` & `~/.claude/writing-profile/state.md`.
